# WeiboArchive（微博数据归档工具）交接文档

> 本文档面向**需求负责人 / 产品 / 后续开发人员**编写，用于完整交接当前项目状态、已实现能力、数据情况、运行方式、关键设计决策与可扩展方向，便于在此基础上提出并评估新需求。

---

> **本文档完整交接当前项目状态；每次发版/修改请在本节「变更记录」顶部追加最新一条，并同步 `CHANGELOG.md` 与 `backend/config.py` 的 `VERSION`。**

## 0. 变更记录（版本履历）

### v1.1.7 · 2026-09-02 · 修复归档浏览搜索后列表不刷新

| 层面 | 改动 | 说明 |
|------|------|------|
| 前端 | `doSearch` 参数严格布尔判断 | `ArchivesView.vue`：`@click="doSearch"` 会把 Event 对象传入首参，`!loadMore` 恒为假导致跳过 `posts=[]` 清空、搜索结果被追加到旧列表后（页面看似无变化）。改为 `const more = loadMore === true` |
| 前端 | 重置清空关键词 + 竞态守卫 + catch | `resetFilters` 补 `keyword.value=''`；`doSearch` 增加 `_searchSeq` 请求序号丢弃过期响应；异常时不再静默停在空态 |
| 版本 | 修订号 +1 | `1.1.6 → 1.1.7`（Bug 修复） |

验证：headless 驱动真实页面四场景全通过（剑来→3、重置→20、剑落→0、回车→3）。

相关文件：`frontend/src/views/ArchivesView.vue`、`CHANGELOG.md`

### v1.1.6 · 2026-09-01 · 修复浏览器"不受支持的命令行标志:--no-sandbox"警告条

| 层面 | 改动 | 说明 |
|------|------|------|
| 后端 | `--no-sandbox` 改为仅 root 时追加 | `backend/login.py` 新增 `_is_root()`（POSIX `os.geteuid()==0`），普通用户桌面省略 `--no-sandbox` 并新增 `--disable-infobars`；Windows 恒 False，EXE 不再传该参数 |
| 版本 | 修订号 +1 | `1.1.5 → 1.1.6`（Bug 修复） |

相关文件：`backend/login.py`、`CHANGELOG.md`

### v1.1.5 · 2026-09-01 · 移除账号管理内嵌二维码，改为引导弹出窗口扫码

| 层面 | 改动 | 说明 |
|------|------|------|
| 前端 | 移除「扫码登录」页内嵌二维码展示 | `AccountManager.vue`：回显二维码扫描不生效（有效二维码在弹出浏览器窗口的 passport.weibo.com 页面），删除误导性内嵌二维码，改为文案引导用户在弹出的浏览器窗口扫码；清理死代码 `refreshQrImg` |
| 版本 | 修订号 +1 | `1.1.4 → 1.1.5`（样式/UI、重构） |

相关文件：`frontend/src/components/AccountManager.vue`、`CHANGELOG.md`

### v1.1.4 · 2026-09-01 · 修复扫码确认读取不到登录 Cookie

| 层面 | 改动 | 说明 |
|------|------|------|
| 后端 | Cookie 读取改走 Network.getAllCookies | `backend/login.py` 新增 `_all_cookies()`，优先 `Network.enable` + `Network.getAllCookies`（含 HttpOnly 凭证 SUB/gsid），失败才回退 `document.cookie`；登录态判定以是否含 SUB/SUBSCRIBE/gsid 为准 |
| 后端 | 确认接口增加登录态校验 | 未检测到登录时返回可操作提示（确认手机已扫码并点击「确认登录」） |
| 版本 | 修订号 +1 | `1.1.3 → 1.1.4`（Bug 修复） |

相关文件：`backend/login.py`、`CHANGELOG.md`

### v1.1.3 · 2026-09-01 · 抑制 Edge 首启附加条款占位页

| 层面 | 改动 | 说明 |
|------|------|------|
| 后端 | 浏览器启动参数抑制首启页 | `backend/login.py` 增加 `--no-first-run`、`--no-default-browser-check`、`--disable-features=msEdgeFirstRunExperience,...` 等参数；启动后关闭多余 page 标签并将微博登录页 `bringToFront`，避免与 Edge 自带"附加条款"占位页并存 |
| 版本 | 修订号 +1 | `1.1.2 → 1.1.3`（Bug 修复） |

相关文件：`backend/login.py`、`CHANGELOG.md`

### v1.1.2 · 2026-09-01 · 修复隐私提示注入未生效

| 层面 | 改动 | 说明 |
|------|------|------|
| 后端 | 注入改为大小写不敏感 + 多 frame 递归 | `backend/login.py`：占位标记改小写（微博页面占位为小写，v1.1.1 大写匹配脱靶）；遍历顶层文档 + 全部同源 iframe；幂等防重复（`#wbar-privacy-panel`），二维码捕获后再注入一次 |
| 版本 | 修订号 +1 | `1.1.1 → 1.1.2`（Bug 修复） |

相关文件：`backend/login.py`、`CHANGELOG.md`

### v1.1.1 · 2026-09-01 · 扫码登录弹窗自定义隐私提示

| 层面 | 改动 | 说明 |
|------|------|------|
| 后端 | CDP 注入隐私文案 | `backend/login.py` 打开登录页后 `Runtime.evaluate` 注入 JS，按文本节点定位并替换占位文本 "This Space Intentionally Blank" 等为「🔒 隐私与安全说明」，Edge/Chrome 通用 |
| 前端 | 二维码下方渲染提示 | `AccountManager.vue` 增加 `.privacy-note` 样式块，扫码流程中始终可见（含兜底截图场景）；同步修正旧注释中残留的 DrissionPage 描述 |
| 版本 | 修订号 +1 | `1.1.0 → 1.1.1`（文案/UI 改动，不发新版本号） |

相关文件：`backend/login.py`、`frontend/src/components/AccountManager.vue`、`CHANGELOG.md`

### v1.1.0 · 2026-09-01 · 扫码登录 / 多账号 / 多用户归档

| 层面 | 改动 | 说明 |
|------|------|------|
| 后端 | 扫码登录改用原生 CDP | `backend/login.py` 用 `websocket-client` 直连本机 Edge/Chrome（外部启动 + 指定调试端口），避免 DrissionPage 4.1 与该环境 Edge 152 的 ws 404 兼容缺陷 |
| 后端 | 扫码确认后解析本人身份 | `auth.py` 扫码成功用 Cookie 解析 UID/昵称入库并切换到该账号工作区；新增「取消扫码」接口释放浏览器 |
| 后端 | 按目标用户隔离 | 每个目标 UID 独立数据库 `workspace/<uid>/weibo_archive.db` 与媒体目录；切换账号/目标自动切换工作区 |
| 前端 | 账号管理器 | 新增 `AccountManager.vue`（扫码/导入/切换/退出/删除/目标 UID），替代原 `CookieDialog.vue` |
| 依赖 | websocket-client 替代 drissionpage | `requirements.txt` 变更；移除未使用的 DrissionPage 以控制包体积 |
| 安全 | 浏览器目录隔离 | Cookie/浏览器 profile 移出 `/media` 可访问路径；`.gitignore` 忽略 `logs/`、`.weibo_browser_profiles/`、`qr_cache/` |

相关文件：`backend/login.py`、`backend/routers/auth.py`(扫码/目标接口)、`backend/workspace.py`、`backend/auth_manager.py`、`frontend/src/components/AccountManager.vue`

---

## 1. 项目概述

**WeiboArchive** 是一款基于 **Python (FastAPI) + Vue 3** 的**纯本地**微博数据归档工具。

- 目标：把用户自己的微博动态（正文、长文、图片、视频、评论、用户头像）**完整、安全地抓取并归档到本地**，并提供可视化浏览与导出能力。
- 定位：极简本地化、零第三方上传、反爬鲁棒、离线可用。
- 典型场景：个人微博备份、内容沉淀、数据分析、知识库构建（导出 Markdown）、离线浏览（导出 HTML）。

> 注意：这是一个**单账号、单用户目录**的本地归档工具，所有数据仅保存在本机，不向任何第三方发送。

---

## 2. 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 后端框架 | FastAPI (async) | RESTful API + WebSocket |
| 网络客户端 | httpx.AsyncClient | HTTP/2、连接池、异步并发 |
| 反爬 | tenacity + 指数退避 + XSRF 自动续期 | 风控/限速处理（DrissionPage 浏览器兜底**未实现**，见 §10） |
| 数据库 | SQLite + FTS5 | 单文件库，全文检索毫秒级 |
| ORM | SQLAlchemy 2.0 (async) | 异步模型 + 中文分词全文索引 |
| 前端 | Vue 3 + Vite + TypeScript | 组件化 SPA |
| UI | Element Plus + ECharts + Pinia | 表格/时间线/图表/状态管理 |
| 桌面壳（可选未接入） | PyWebView | 需求文档中的可选壳，当前未使用 |
| 打包（未完成） | Nuitka / PyInstaller | 见 §10 待办 |

---

## 3. 系统架构

**本地后端服务 + 浏览器前端**：

1. 启动：运行 `start.sh`（或 `launch.py`），先构建前端（若无 `frontend/dist`），再启动 FastAPI 服务。
2. 服务监听 **`127.0.0.1:8964`**，初始化 SQLite 数据库与 FTS5 索引。
3. 前端托管：`backend/main.py` 通过 `StaticFiles` 挂载 `frontend/dist`；`/media/*` 挂载 `workspace/` 目录（媒体文件访问）。
4. 启动后自动调用浏览器打开 `http://127.0.0.1:8964`。
5. 前端通过 RESTful API 读写数据，通过 **WebSocket**（`/api/task/ws`）接收任务日志与进度实时推送。

```
浏览器 / 本地前端
    │  REST API (JSON) + WebSocket
后端 FastAPI (127.0.0.1:8964)
    ├── 归档任务调度器 (task_manager)
    ├── 微博爬虫 (client / parser / media_downloader)
    └── SQLite (SQLAlchemy async) + 本地媒体文件
```

---

## 4. 目录结构

```
webo_archive/
├── backend/                     # Python 后端 (FastAPI)
│   ├── main.py                  # 启动入口 & 生命周期 & 静态托管
│   ├── config.py                # 配置（端口/路径/并发/常量）
│   ├── database.py              # SQLAlchemy 模型 + FTS5 全文索引 + 工具查询
│   ├── schemas.py               # Pydantic 模型
│   ├── routers/
│   │   ├── auth.py              # Cookie 导入/校验/状态
│   │   ├── task.py              # 任务启动/停止/状态 + WebSocket
│   │   ├── posts.py             # 微博列表/搜索/媒体/评论/汇总/年月
│   │   └── export.py            # 数据导出（HTML/Markdown）
│   ├── scraper/
│   │   ├── client.py            # httpx 异步客户端 + 重试 + XSRF 续期 + 下载
│   │   ├── parser.py            # 微博/长文/图片/视频/评论树解析入库
│   │   ├── media_downloader.py  # 媒体 + 头像下载
│   │   └── task_manager.py      # 后台任务状态机 + 并发 + 进度
│   ├── templates/archive.html   # HTML 离线包模板
│   └── utils/logger.py          # 日志（Cookie 脱敏）
├── frontend/                    # Vue 3 前端
│   └── src/
│       ├── views/               # Dashboard/Archives/Media/Tasks
│       ├── components/          # PostCard/CookieDialog
│       ├── api/ · stores/ · router/ · layouts/
├── workspace/<uid>/             # 运行时数据（git 忽略）
│   ├── weibo_archive.db         # SQLite 数据库
│   └── resources/{pic,video,avatar}/  # 媒体/头像文件
├── workspace/default/           # 当前使用的用户目录（uid=default）
├── requirements.txt / start.sh / launch.py / build.py
└── 需求文档.md / 可能的技术路线.md
```

> **git 状态**：源码已托管于 `git@github.com:xskyyn/weibo_archive_new.git`（分支 `main`）。`workspace/`、`cookie.json`、`.venv`、`frontend/dist`、`node_modules` 均已加入 `.gitignore`，**不会被提交**。

---

## 5. 已实现功能清单

### 5.1 认证 / Cookie 管理
- 前端导入 Cookie（对话框），后端校验并自动提取/续期 **XSRF-TOKEN**。
- `weibo.com` 接口真实认证可用（`SUB`/`SUBP`/`WBPSESS`/`XSRF-TOKEN`）。

### 5.2 异步归档引擎
- 基于 `asyncio.Semaphore` 并发控制（默认 5），配合随机延迟与 `tenacity` 指数退避。
- 微博列表分页抓取、增量去重（按 `mid`）。
- 图片异步流式下载；特殊封装自动调 `ffmpeg`（视频逻辑就绪，见 §10）。
- **评论树**：通过 `weibo.com/ajax/statuses/buildComments` 抓取**一级评论（分页）+ 二级评论（预载+分页补全）**。

### 5.3 数据存储
- SQLite 关系模型：`User` / `Post` / `Media` / `Comment`。
- **FTS5 全文索引**（`posts_fts` 通过触发器同步 `posts`），结合 jieba 中文分词，支持中文关键词检索。
- 转发处理：`Post.is_own` 标记本人/转发来源原博（见 §7 关键决策）。

### 5.4 前端页面
- **仪表盘**：数据概览 + 发博热力图。
- **归档浏览**：无限滚动时间线 + 全文搜索（高亮）+ 年月/媒体筛选 + 评论查看。
- **媒体时光轴**：瀑布流画廊 + Lightbox。
- **任务中心**：启动/停止 + 验证码交互占位 + WebSocket 实时日志/进度。

### 5.5 数据导出
- HTML 离线包导出（`backend/templates/archive.html`）。
- Markdown 导出。

---

## 6. 本次交接已完成的关键工作（含真机归档）

在交接本版本前，已完成一轮**真实账号（uid 7769842070）的完整归档**并做了多处修正：

| 事项 | 说明 |
|------|------|
| 真机抓取 | 抓取该账号全部微博，翻页到最后一页，**无触发风控/验证码** |
| 评论抓取 | 接通 `weibo.com/buildComments`，抓本人微博的一二级评论 |
| **转发内容修正** | 识别出「转发的他人内容」不能算作本人内容，新增 `is_own` 标记 |
| **评论归属修正** | 清理了之前误抓的他人爆款博评论（曾达 4536 条，占 98%），当前评论 100% 归属本人 |
| **媒体补全** | 修复「转发的原博图片未下载」，新增 `download_all_missing()`，将原博图片下载且随转发卡片展示 |
| 头像本地化 | 新增用户头像下载（`tvax2.sinaimg.cn` 格式验证可用），本地回填并可离线展示 |
| git 托管 | 初始化仓库并推送到 GitHub（SSH） |

---

## 7. 关键设计决策（新需求评估时务必了解）

1. **`Post.is_own` 字段**：`is_own=True` 表示用户本人发布的微博；`is_own=False` 表示因「转发」而递归保存的**他人原博**。
   - 本人微博才是主数据，纳入统计/搜索/热力图/时光轴/评论抓取。
   - 他人原博保留为独立记录（供转发卡片展示其图片），但**不计入统计、不出现在本人时间线/搜索、不抓其评论**。
2. **评论抓取范围 = 仅 `is_own=True` 的微博**。已实现，避免抓别人博的评论。
3. **媒体范围**：`download_all_missing()` 会下载所有未下载媒体（含他人原博图片），确保转发卡片图片可见。
4. **评论页数上限**：每条微博评论最多抓 `COMMENT_MAX_PAGES = 20` 页（约 400 条），防止爆款博（几十万评论）拉飞接口。
5. **FTS5 仅索引 `posts.text`**，搜索也限定 `is_own=True`。

---

## 8. 当前归档数据状态（uid 7769842070，单账号实测）

数据库文件：`workspace/default/weibo_archive.db`

| 指标 | 数值 |
|------|------|
| 全部微博记录 | 1466 |
| 本人微博（`is_own=1`） | 1380 |
| 转发来源他人原博（`is_own=0`） | 86 |
| 评论（本人微博） | 166 |
| 媒体文件 | 1334，**已 100% 下载（缺失 0）** |
| 视频 | 0（该账号无视频博；逻辑就绪） |
| 用户数 | 3743 |
| 头像已下载 | 3743（100%） |

---

## 9. 如何运行 / 联调

### 9.1 启动
```bash
# 首次会创建 .venv、安装依赖、构建前端
./start.sh
# 或手动：
#   .venv/bin/python -m backend.main
```
自动打开 `http://127.0.0.1:8964`。

### 9.2 准备 Cookie
- 需要微博登录后的 Cookie（`weibo.com` 有效，含 `SUB`/`SUBP`/`WBPSESS`/`XSRF-TOKEN`）。
- 可放入 `/opt/webo_archive/cookie.json`，或在前端「Cookie 对话框」导入。
- **注意**：Cookie 属敏感凭证，仅存本机，不入 git。

### 9.3 触发归档
- 前端「任务中心」点击启动（需传入目标 uid，如 `7769842070`）。
- 观察 WebSocket 实时日志，直到 `completed`。

### 9.4 主要 API 一览
| 方法/路径 | 说明 |
|-----------|------|
| `GET /api/stats` | 数据统计（本人口径） |
| `GET /api/posts` | 微博时间线分页（is_own，支持年月/媒体筛选） |
| `GET /api/posts/search?keyword=` | FTS5 中文全文搜索 |
| `GET /api/posts/timeline` | 发博热力图数据 |
| `GET /api/media` | 媒体时光轴 |
| `GET /api/posts/{id}/comments` | 单条微博评论 |
| `POST /api/task/start|stop` | 任务启停 |
| `GET /api/task/status` | 任务状态 |
| `WS /api/task/ws` | 任务日志/进度实时推送 |
| `GET /api/export/html|md` | 数据导出 |
| `GET /api/auth/config` | Cookie 状态 |

---

## 10. 已知限制与可扩展方向（供提出新需求）

### 已实现但存在边界
1. **视频**：解析与下载逻辑已写好（`page_info.urls` + ffmpeg 转封装），但目标账号无视频，**未在真机上验证**；需找一个含视频的账号实测。
2. **单账号**：仅支持单个目标 uid 归档；需求文档设想的「多账号 Cookie 池 + 轮换」未实现。
3. **Cookie 失效的浏览器降级（DrissionPage）**：未接入。需求文档 Phase 4 的进阶项（无头浏览器自动扫码续 Cookie / JS 逆向兜底）尚未实现。
4. **验证码人机协作**：后端有验证码检测与状态机（暂停→等待），但**前端弹窗交互为占位**，未完整闭环。
5. **打包发布**：`build.py` 存在但未实际执行 Nuitka/PyInstaller 打包；当前以源码运行。

### 建议优先的增强方向（候选）
- **增量同步**：优化为「仅抓取新增/变更」（当前每次会重新翻页去重，但无「断点增量」语义）。
- **多账号 / 多用户归档**与**账号切换隔离**（按 uid 目录已具备基础）。
- **数据导出增强**：导出为 Excel/CSV、JSON 完整备份、图片选择导出。
- **数据分析**：互动（点赞/转发/评论）排行、词云、年份趋势等更多 ECharts 可视化。
- **搜索增强**：支持全文搜索命中高亮、按用户/时间段/媒体类型组合筛选。
- **DrissionPage 降级**与**扫码续 Cookie**，提升长期运行的稳定性。
- **恢复断点续传**的容错（网络中断后任务从上次位置继续）。
- **清理孤儿数据**：`users` 表中含来自已排除他人原博评论的用户，可加「仅保留与本人数据存在关联」的清理逻辑（当前为 100% 下载头像的空转开销）。

---

## 11. 交接信息

- **代码仓库**：`git@github.com:xskyyn/weibo_archive_new.git`（分支 `main`），当前提交 `1b8eba1`。
- **本地路径**：`/opt/webo_archive`
- **运行环境**：Ubuntu 20.04+（Linux），Python 3.10+，Node.js（构建前端）。当前运行时数据位于 `workspace/default/`。
- **访问入口**：`http://127.0.0.1:8964`
- **敏感项**：登录凭证（GitHub token、微博 Cookie）请妥善保管；文档如需外发，建议脱敏（见 §9.2 与 §11 安全提醒）。

### 安全提醒
- `cookie.json` 与 `workspace/` 已 git 忽略，切勿强制提交。
- 日志系统已做 Cookie 脱敏（`backend/utils/logger.py`）。
- 此前对话中出现过一个 GitHub token，**建议已吊销**；本项目推现已改走 SSH，无需该 token。