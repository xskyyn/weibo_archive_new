这是一份完全基于 方案 A（FastAPI + Vue 本地 Web 服务架构） 量身定制的《微博数据归档工具 (WeiboArchive) 需求规格说明书》。这份文档充分发挥了 Python 在数据清洗、异步 IO 和反爬对抗上的生态优势。

📄 微博数据归档工具 (WeiboArchive) 需求规格说明书

项目概述 (Project Overview)

1.1 项目名称
WeiboArchive (微博归档)

1.2 项目定位
一款基于 Python (FastAPI) + Vue 3 架构的现代化本地数据归档工具。利用 Python 强大的异步网络库和反爬生态，将用户的微博动态、长文、图片、视频及多级评论安全、完整地归档到本地，并提供丰富的可视化浏览与导出功能。

1.3 核心设计理念
反爬降维打击：利用 Python 生态（httpx + DrissionPage），无缝切换 API 请求与浏览器自动化，从容应对微博复杂的风控策略。
极致本地化：所有数据存储在本地 SQLite，服务仅在本地回环地址（127.0.0.1）运行，杜绝隐私泄露。
高并发与高稳定：基于 asyncio 实现非阻塞 IO，配合智能限速与断点续传，保障海量数据的稳定抓取。部分实现逻辑可以参考已有的QzoneArchive项目。也可以参考已经实现了的项目run.py。或者直接调用浏览器访问微博的网站，我会给予你访问我的账号密码的权限，你来查找对应的信息。
有一个可能的技术路线可以参考在 可能的技术路线.md文档中

技术栈选型 (Technology Stack)
层级   技术选型   核心优势与说明
后端框架   FastAPI   原生异步支持，自动生成 OpenAPI 文档，开发效率极高。

网络客户端   httpx (AsyncClient)   支持 HTTP/2 和连接池复用，比 requests 性能提升数倍。

反爬兜底   DrissionPage   国产神器，报文收发与浏览器控制无缝切换，专治各种 JS 逆向和滑块验证码。

数据库   SQLite + FTS5   零配置单文件数据库，利用 FTS5 扩展实现百万级数据的毫秒级全文检索。

ORM 框架   SQLAlchemy 2.0 (Async)   强类型提示，安全的 SQL 构建，完美支持异步数据库操作。

前端框架   Vue 3 + Vite + TS   现代化组件化开发，响应式数据绑定，构建速度极快。

UI 组件库   Element Plus   开箱即用的企业级组件，提供完善的表格、分页、对话框和时间线组件。

数据可视化   ECharts   渲染发博热力图、词云、互动排行榜等复杂图表。

桌面外壳   PyWebView (可选)   轻量级 Python 库，用于将本地 Web 服务包装成原生桌面窗口，无需引入沉重的 Electron。

打包分发   Nuitka / PyInstaller   将 Python 后端编译/打包为单一可执行文件（.exe / binary），隐藏源码。

系统架构设计 (System Architecture)

采用 “本地后端服务 + 浏览器/轻量外壳前端” 的架构：

启动流程：用户双击运行打包好的 WeiboArchive.exe。
后端初始化：Python 脚本在后台启动 FastAPI 服务（监听 127.0.0.1:8964），初始化 SQLite 数据库，并启动后台异步任务调度器。
前端展示：程序自动调用系统默认浏览器打开 <http://127.0.0.1:8964（或使用> PyWebView 弹出一个无边框的本地桌面窗口），加载 Vue 前端静态资源。
数据交互：前端通过 RESTful API 和 WebSocket（用于实时推送抓取日志和进度）与后端通信。

功能需求 (Functional Requirements)

4.1 认证与登录模块 (Authentication)

Cookie 导入与校验：支持导入 cookie.json。后端启动时调用 /api/config 校验有效性，自动提取并刷新 XSRF-TOKEN。
智能 Cookie 轮换：支持配置多个账号的 Cookie 池，当某个 Cookie 触发风控（如返回 418/403）时，自动冷却并切换到下一个。
DrissionPage 扫码登录 (进阶)：内置一个隐藏接口，当 Cookie 失效时，前端弹出二维码，后端使用 DrissionPage 开启无头浏览器模拟扫码流程，自动提取最新 Cookie 存入本地。

4.2 异步归档引擎 (Async Archive Engine)

4.2.1 核心抓取逻辑
异步并发控制：使用 asyncio.Semaphore(5) 限制最大并发请求数，避免瞬间打满带宽或触发风控。
微博列表分页：基于 since\_id 游标异步翻页，解析 card\_type=9 (普通) 和 card\_type=11 (卡片组)。
关联内容并发抓取：对于单条微博，使用 asyncio.gather 并发抓取其长文、图片、视频和评论，大幅提升单条微博的处理速度。

4.2.2 媒体下载器
图片：异步流式下载（stream=True），支持 JPG/PNG/GIF/WebP，自动重试。
视频/LivePhoto：提取最高清 MP4/MOV 链接。对于 m3u8 或特殊封装格式，后端自动调用本地 ffmpeg 进程进行异步转封装。

4.2.3 评论树递归
异步递归抓取一级评论 (hotflow) 和二级评论 (hotFlowChild)，直到 max\_id=0。
将评论与用户信息一并入库，构建完整的互动关系网。

4.2.4 风控与异常处理 (核心亮点)
指数退避重试：引入 tenacity 库，遇到网络超时或 5xx 错误时，按 2s, 4s, 8s 自动重试。
验证码人机协作：
当 API 返回 code=-100 (需要验证码) 时，后端通过 WebSocket 向前端推送警报。
前端弹出模态框，展示验证码链接或图片。
用户在浏览器中手动完成验证后，点击“继续”，后端恢复异步抓取任务。
DrissionPage 降级策略：当 API 接口被彻底封禁（如强制要求复杂的 JS 动态参数 X-S）时，后端自动将请求交由 DrissionPage 接管，通过真实浏览器环境获取数据，牺牲部分速度换取 100% 的成功率。

4.3 数据存储 (Data Storage)

4.3.1 SQLite 关系模型 (SQLAlchemy 定义)
核心表结构示意
class User(Base):
id: Mapped\[int] = mapped\_column(primary\_key=True)
screen\_name: Mapped\[str]
profile\_image\_url: Mapped\[str]

class Post(Base):
id: Mapped\[int] = mapped\_column(primary\_key=True)
mid: Mapped\[str] = mapped\_column(unique=True)
user\_id: Mapped\[int] = mapped\_column(ForeignKey("users.id"))
created\_at: Mapped\[datetime]
text: Mapped\[str]          # 清洗后的纯文本
raw\_html: Mapped\[str]      # 原始 HTML 正文
reposts\_count: Mapped\[int]
comments\_count: Mapped\[int]
attitudes\_count: Mapped\[int]
retweeted\_status\_id: Mapped\[Optional\[int]]

class Media(Base):
id: Mapped\[int] = mapped\_column(primary\_key=True)
post\_id: Mapped\[int] = mapped\_column(ForeignKey("posts.id"))
type: Mapped\[str]          # 'pic', 'video', 'livephoto'
url: Mapped\[str]
local\_path: Mapped\[str]

class Comment(Base):
id: Mapped\[int] = mapped\_column(primary\_key=True)
post\_id: Mapped\[int] = mapped\_column(ForeignKey("posts.id"))
user\_id: Mapped\[int] = mapped\_column(ForeignKey("users.id"))
text: Mapped\[str]
parent\_id: Mapped\[Optional\[int]] # 二级评论指向一级评论的 ID

4.3.2 全文搜索引擎 (FTS5)
在 SQLite 中建立 FTS5 虚拟表，映射 posts 表的 text 字段。
支持中文分词（结合 jieba 或 SQLite 内置的 unicode61 tokenizer），实现类似“搜索 2018 年关于 猫 的微博”的毫秒级查询。

4.4 数据展示与导出 (Visualization & Export)

4.4.1 前端页面规划
仪表盘 (Dashboard)：
数据概览卡片（总微博、总评论、总媒体数）。
ECharts 发博时间热力图（按小时/星期统计活跃度）。
互动排行榜（最常评论你的 Top 10 好友）。
归档浏览 (Archives)：
类似 Twitter/微博的无限滚动时间线。
顶部全局搜索框（调用 FTS5 接口），搜索结果高亮显示。
支持按年份、月份、是否有图/视频进行多维筛选。
媒体时光轴 (Media)：
瀑布流画廊展示所有图片和视频封面。
点击弹出 Lightbox 预览，视频支持直接在页面内播放。
任务与日志 (Tasks)：
显示当前抓取进度条。
实时滚动日志控制台（通过 WebSocket 接收后端 print/logging 输出）。

4.4.2 数据导出
HTML 离线包：使用 Jinja2 模板引擎，将数据库数据渲染为纯静态 HTML + 本地媒体文件夹，打包成 ZIP 供用户离线浏览。
Markdown 导出：将长文微博转换为 Markdown 格式，方便用户导入 Obsidian 或 Notion 构建个人知识库。

项目目录结构 (Project Structure)

WeiboArchive/
├── backend/                    # Python 后端 (FastAPI)
│   ├── main.py                 # FastAPI 启动入口 & 生命周期管理
│   ├── config.py               # 配置管理 (端口、路径、并发数)
│   ├── database.py             # SQLAlchemy 模型定义 & 数据库初始化
│   ├── schemas.py              # Pydantic 数据校验模型
│   ├── routers/                # API 路由
│   │   ├── auth.py             # 登录与 Cookie 管理
│   │   ├── archive.py          # 归档任务控制 (启动/暂停/状态)
│   │   ├── posts.py            # 微博数据查询、搜索、分页
│   │   └── export.py           # 数据导出接口
│   ├── scraper/                # 爬虫核心引擎
│   │   ├── client.py           # httpx 异步客户端封装 & 请求重试
│   │   ├── parser.py           # 微博脏数据解析与清洗
│   │   ├── media\_downloader.py # 异步媒体文件下载器
│   │   └── drission\_fallback.py# DrissionPage 浏览器降级处理
│   └── utils/                  # 工具函数 (日志、FFmpeg 调用等)
├── frontend/                   # Vue 3 前端
│   ├── src/
│   │   ├── views/              # 页面组件 (Dashboard, Archives, Media, Tasks)
│   │   ├── components/         # 通用 UI 组件 (微博卡片、评论树、搜索框)
│   │   ├── api/                # Axios 封装与后端接口定义
│   │   ├── stores/             # Pinia 状态管理
│   │   └── App.vue
│   ├── package.json
│   └── vite.config.ts
├── workspace/                  # 运行时数据目录 (自动创建)
│   └── {uid}/                  # 按用户 UID 隔离
│       ├── weibo\_archive.db    # SQLite 数据库文件
│       └── resources/          # 媒体文件存储 (pic/, video/)
├── requirements.txt            # Python 依赖
└── build.py                    # 打包脚本 (调用 Nuitka/PyInstaller)

非功能需求 (Non-Functional Requirements)

6.1 性能指标
并发抓取：在 5 并发下，API 请求吞吐量达到 2-3 页/秒。
查询响应：10 万条微博规模下，FTS5 全文搜索响应时间 < 200ms。
内存控制：后端服务常驻内存控制在 150MB - 300MB 之间（得益于异步流式处理和 SQLAlchemy 的惰性加载）。

6.2 安全与隐私
本地闭环：所有网络请求仅发往 \*.weibo.cn 和 \*.sinaimg.cn，绝不向任何第三方服务器发送数据。
凭证保护：Cookie 仅保存在本地 workspace 目录下的加密配置中，日志系统自动脱敏（Masking）Cookie 值。

6.3 部署与兼容性
运行环境：Ubuntu 20.04+。
依赖极简：用户无需安装 Python 环境，双击打包后的可执行文件即可运行（后端打包为独立二进制，前端编译为静态文件嵌入）。
外部依赖：若需处理特殊视频格式，程序会自动检测并提示用户安装 FFmpeg，或内置一个轻量级的 FFmpeg 二进制文件。

开发路线图 (Roadmap)

Phase 1: 基础设施与数据模型 (Week 1)
\[ ] 搭建 FastAPI 项目骨架，配置 CORS 和全局异常处理。
\[ ] 使用 SQLAlchemy 2.0 定义数据库模型，实现 SQLite 初始化与 FTS5 索引创建。
\[ ] 封装 httpx.AsyncClient，实现带随机延迟和指数退避的请求中间件。

Phase 2: 核心爬虫引擎 (Week 2-3)
\[ ] 实现 Cookie 校验与 XSRF-TOKEN 自动刷新逻辑。
\[ ] 编写微博列表、长文、评论树的异步解析器（处理各种脏数据边界情况）。
\[ ] 实现异步媒体下载器，集成 ffmpeg 处理视频流。
\[ ] 引入 asyncio.Semaphore 和 tenacity 完善并发控制与重试机制。

Phase 3: 前端界面与联调 (Week 4-5)
\[ ] 搭建 Vue 3 + Element Plus 前端项目。
\[ ] 实现仪表盘统计图表和时间线无限滚动列表。
\[ ] 通过 WebSocket 实现后端日志到前端控制台的实时推送。
\[ ] 实现验证码弹窗的人机协作交互流程。

Phase 4: 导出、打包与优化 (Week 6)
\[ ] 实现 HTML 离线包和 Markdown 导出功能。
\[ ] 引入 DrissionPage 作为高级反爬的兜底方案。
\[ ] 使用 Nuitka 将后端编译为 C 扩展或独立可执行文件。
\[ ] 编写跨平台打包脚本，进行 Windows  的真机测试。

注意：当需要管理员权限的时候，可以向我申请，当需要账号测试的时候，也要像我申请，比如获取cookie之类的
